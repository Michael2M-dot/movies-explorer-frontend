import React from 'react';

const Loader = () => (
  <div className='loader'>
    <div className='loader__container'>
      <span className='loader__round' />
    </div>
  </div>
);

export default Loader;
